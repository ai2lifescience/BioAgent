# Commands required inside the WDL task image.
name: metagenomic-qc

channels:
  - conda-forge
  - bioconda
  - nodefaults

dependencies:
  - bash >=4.4
  - coreutils
  - python >=3.10
  - fastp >=0.23
  - kraken2 >=2.1
  - bowtie2 >=2.5
