# Commands required inside the WDL task image.
name: consensus-risk-assessment

channels:
  - conda-forge
  - bioconda
  - nodefaults

dependencies:
  - bash >=4.4
  - coreutils
  - openjdk >=11
  - snpeff
